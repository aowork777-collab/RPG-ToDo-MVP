# Game assets

モンスター画像は既存の `assets/monsters/*.webp` を使用します。

現在のプレイヤーは Canvas 上の絵文字キャラクターで表示され、待機、移動、攻撃、被弾、敗北の動きが付きます。プレイヤー画像を追加する場合は `src/game/Game.mjs` の `imageUrl` に画像URLを設定してください。

本格的なフレームアニメーションを追加するときは、このフォルダへ同じフレームサイズのスプライトシートを置きます。
