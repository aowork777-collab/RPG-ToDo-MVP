# 習慣・仲間・冒険アップデート（公開ベータ）

## 追加機能

- 「今日・記録・冒険・仲間・自分」の5画面ナビゲーション。
- 完了履歴カレンダー、功績、気分・ひとこと、週の振り返り。
- 「休む日」と未記録の区別。連続日数ではなく、自分で決めた週の達成日数を目標にする設計。
- 大きなタスクを「2分でできる一歩」に変更。変更時にはXPを増やさず、実際の完了時に獲得。
- 表示名・アバター・目標・興味タグのプロフィール。
- Googleログイン（ToDo・記録・冒険はログイン不要）。
- 招待承認制の非公開共有ボード。閲覧者・編集者の区別と権限取り消し。
- タグのサークル、1日1回の達成報告、エール、管理者による投稿削除。
- 自分専用のクラウド保管・復元。保存競合チェックと復元前の端末バックアップ。
- 100ステージをつなぐ10章の冒険マップ、章クリアの遺物、モンスター図鑑。
- ToDoのPLAYER LEVEL 5・10で解放される技とアニメーション。

## 変わらないこと

- 個人ToDo・毎日タスク・XPと冒険の保存キーを維持。
- PLAYER LEVELはToDoのXPから計算。ゲーム側からToDoのXPを書き換えない。
- 個人のToDoやメモを自動公開しない。共有は選んだタスク名だけ。
- 共有ボードの完了では個人XPを増やさない。「自分のToDoへ」コピーして実際に取り組む。
- 日付不明の昔の完了記録に、架空の完了日を付けない。

## 使い方

1. 個人の表示名や興味タグは「自分」で設定。
2. 「仲間」から「Googleでログイン」。初回も同じボタンを使用。
3. 共有相手もログインし、「アカウント・保管」にある招待先IDを伝える。
4. ボード管理者がそのIDを指定し、閲覧または編集権限で招待。
5. 相手が「共有ボード」で承認するとタスクが見える。
6. タグの仲間とはサークルに参加し、投稿する文章を自分で入力して報告。

プロフィールは端末保存と仲間向け保存を分けています。タグ検索への掲載も自分で有効にします。
クラウド保管は**手動バックアップ**です。自動同期ではありません。

## 検証済み

- 56件の自動テスト：既存ToDo・ゲーム・PWA、履歴移行、休む日、技解放、DOM操作、OAuth戻り先、保存失敗、競合、ログアウトなど。
- ページ・モジュール・アセット参照チェック。
- ローカルHTTP配信：ToDo・hub・battle・認証モジュール・CSS・SDKが200。配信対象外の設定ファイルは404。
- ホストDBのトランザクションテスト：管理者、招待承認前、閲覧者、編集者、無関係ユーザー、権限取消後、匿名、非公開保管、権限昇格の禁止、重複エールの禁止。テストデータはロールバック済み。
- Supabaseセキュリティ診断：指摘0件。
- Googleプロバイダー有効化、認証開始URLからGoogleアカウント画面への302を確認。

## 公開後に本人の操作で確認すること

- 実アカウントでのGoogle同意・認証完了・アプリへの帰還は、自動テストでは代替していません。
- 2つの許可済みテストアカウントで招待・承認・閲覧・編集・取消を確認してください。
- スマートフォン実機とデスクトップの描画・操作確認は未完了です。この実行環境ではブラウザ実行と取得に失敗したため、DOMテストのみで実際の描画確認をしたとは扱っていません。
- Google Auth PlatformのAudienceがTestingの場合はテストユーザーが対象です。一般公開時は公開対象の設定も確認してください。

## 現在の制限

- 共有データはボタンによる再取得。リアルタイム同期ではありません。
- サークル一覧・検索は最新100件、投稿表示は今週の最新60件。大規模運用向けのページング・集計最適化は今後の課題です。
- サークルの説明・タグはログイン利用者に公開。投稿は参加者向けです。退会だけでは過去投稿を削除しません。
- 取り消した権限での新たな取得・更新は禁止しますが、相手がすでに見た・コピーした内容は回収できません。
- アカウント自体の削除は現在管理者対応。利用者自身でクラウド保管・自分の投稿・自分のボード等は削除できます。
- 公開ランキングや対戦用の不正対策はありません。個人のゲーム進行はブラウザに保存します。

## 認証と運用

Supabaseプロジェクト：`rxvdnikizkyfavczoyym`（Tokyo）。クライアントは公開用publishable keyのみを使用し、アクセス権はDBのRLSで制御します。

- Site URL：`https://aowork777-collab.github.io/RPG-ToDo-MVP/`
- アプリへの許可済み戻り先：`https://aowork777-collab.github.io/RPG-ToDo-MVP/hub.html`
- Google側の認証リダイレクト：`https://rxvdnikizkyfavczoyym.supabase.co/auth/v1/callback`
- Google Client SecretはSupabase側のみ。GitHubやHTMLには入れません。
- GoogleログインはSMTP不要。メール・パスワード登録のUIは提供していません。
- ローカルでGoogleログインを試す場合だけ、そのURLをSupabaseの許可済み戻り先に追加します。
- PKCEを使用。ブラウザSDKがURL中のコードを交換し、処理後に認証パラメーターをURLから除去します。

[Supabase公式Google認証ガイド](https://supabase.com/docs/guides/auth/social-login/auth-google)

DB定義：`supabase/migrations/20260915130730_habit_community.sql`（適用済み）。アクセス検証：`supabase/tests/access.sql`。

## ローカル確認

`npm start` またはポートが使用中なら `PORT=8092 npm start`。

`npm test` / `npm run check`。

`npm ci` 後、`node scripts/build-vendor.mjs` で固定バージョンのSDKを再バンドルできます。静的配信のみなら依存ライブラリの再インストールは不要です。開発・再バンドルにはNode.js 22以上を使用してください。
